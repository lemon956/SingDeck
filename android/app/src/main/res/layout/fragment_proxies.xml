<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_dark">

    <!-- 1. Outbound Groups Horizontal Tabs -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_group_tabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:clipToPadding="false"
        android:orientation="horizontal" />

    <!-- 2. Active Group Summary Card -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="4dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp"
        android:background="@drawable/bg_card_dark">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tv_active_group_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="PROXY"
                    android:textColor="@color/text_primary"
                    android:textSize="14sp"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/tv_active_group_type"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="6dp"
                    android:text="Selector"
                    android:textColor="@color/status_cyan"
                    android:textSize="11sp"
                    android:paddingStart="5dp"
                    android:paddingEnd="5dp"
                    android:paddingTop="1dp"
                    android:paddingBottom="1dp"
                    android:background="@drawable/bg_badge_idle" />
            </LinearLayout>

            <TextView
                android:id="@+id/tv_cur_active_node"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="当前生效: 香港 01"
                android:textColor="@color/text_secondary"
                android:textSize="11sp" />
        </LinearLayout>

        <Button
            android:id="@+id/btn_test_all_nodes"
            android:layout_width="wrap_content"
            android:layout_height="34dp"
            android:text="⚡ 一键测速"
            android:textColor="@color/text_primary"
            android:textSize="11sp"
            android:textStyle="bold"
            android:background="@drawable/bg_btn_primary"
            android:paddingStart="12dp"
            android:paddingEnd="12dp" />
    </LinearLayout>

    <!-- 3. Tooling Bar (Search, Layout Toggle, Sort, Refresh) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="8dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <EditText
            android:id="@+id/et_search_nodes"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:minHeight="48dp"
            android:layout_weight="1"
            android:hint="搜索节点..."
            android:textColor="@color/text_primary"
            android:textColorHint="@color/text_muted"
            android:textSize="12sp"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:background="@drawable/bg_badge_idle"
            android:singleLine="true" />

        <ImageButton
            android:id="@+id/btn_toggle_layout"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="6dp"
            android:src="@android:drawable/ic_menu_agenda"
            app:tint="@color/text_secondary"
            android:contentDescription="切换节点列表布局"
            android:background="@drawable/bg_badge_idle" />

        <ImageButton
            android:id="@+id/btn_sort_nodes"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="6dp"
            android:src="@android:drawable/ic_menu_sort_by_size"
            app:tint="@color/text_secondary"
            android:contentDescription="切换节点排序方式"
            android:background="@drawable/bg_badge_idle" />

        <ImageButton
            android:id="@+id/btn_refresh_proxies"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="6dp"
            android:src="@android:drawable/ic_popup_sync"
            app:tint="@color/text_secondary"
            android:contentDescription="刷新核心节点状态"
            android:background="@drawable/bg_badge_idle" />
    </LinearLayout>

    <!-- 4. Candidate Nodes RecyclerView with native Swipe and Pull-to-refresh -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipe_refresh"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_nodes_grid"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingBottom="16dp" />
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
</LinearLayout>
